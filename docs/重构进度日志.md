# 客户端重构日志

2026-09-08 · S0：已读取根目录 docs 技术方案。保留 Cocos Creator 3.8.8 与微信小游戏发布目标，计划分离棋盘几何、真实 3D 表现、网络消息序列与 UI；复刻原图，保留手动校准，加入双骰选择、颜色意愿与个人视角。基线工作树干净。

跨仓阶段计划见 `../../docs/重构进度日志.md`。下一步：同步 S1 权威协议，实施 S2 棋盘几何与 S3 表现。

## S2：精确描图与协议镜像

- `BoardGeometry.ts` 从原图测量一个 13 格扇区，按旋转对称构成 52 格外环；与机场、起飞、跑道一起提供 96 个确定锚点。
- `BoardArtwork.ts` 使用同一组矢量图元绘制棋盘，`scripts/export-board.ts` 导出 SVG 棋盘、锚点审阅图和完整 JSON 坐标；原 PNG 仍保留。
- `BoardLayout.ts` 默认使用描图格心，手动校准坐标继续作为覆盖值。取消单架棋子的额外偏移；机场编号固定，后续叠子由高度表现。
- 四阵营视角变换和逆变换为纯函数，后续接入 3D 和校准拖动。
- 已同步服务端协议 v2 / PathData。此阶段是几何和契约恢复点，双骰交互与新棋盘运行时接线在 S3 完成。

## S3：3D 表现与联机交互完成（2026-09-09）

- 拆分 `BoardScene3D / TokenMeshes / DiceView3D / MotionTimeline / PresentationQueue / BoardCalibrationView / MatchHud`，棋盘控制器只组织表现，不重复计算规则。
- 真实网格飞机与六面骰子通过正交相机、透明 RenderTexture 合成到 2D 矢量棋盘；移动起伏、跳跃、虫洞飞行、吃子翻滚击回和终点返航已接通。
- 两枚骰子抛入棋盘、碰撞回弹、定面，实体骰子和点数按钮都可选择。服务器 `rollId` 校验覆盖选骰和移动；客户端命令等待和 FIFO 消息播放避免快照抢跑。
- 房间颜色意愿、准备状态、快速匹配置灰、个人机场左下视角、技能占位接口接通；原管理校准流程支持旋转后的逆变换保存。
- 修复 Cocos loose Babel 对 Set 展开的转译问题、RenderTexture Y 镜像、旧 Web 模板固定子窗口导致的拉伸/裁切、旧 socket 迟到关闭影响新连接。
- 裁剪未使用的物理、骨骼、粒子等引擎模块。Web 与微信均能构建，微信包体约 3.53 MB（含原 PNG）；最终包体复检与发布说明归 S4。
- 验证：客户端 5 项测试；真实双人 WebSocket + Chrome 点击验收通过，含待选骰重连、两种视角、吃子/虫洞/终点/回弹表现样例、落点误差 < 0.01 棋盘单位、旋转校准拖动、竖屏重排。截图位于 `docs/verification`。
- 场景验收中的 4 个特殊动作由真实服务端规则生成本地表现夹具；它们不是实际网络对局录制。正常开房、选骰、起飞则全部经过真实服务器。

## S4：构建、验收与交付文档完成（2026-09-09）

- S3 客户端实现恢复点 `38a4c2e`，配套服务端恢复点 `bd0b7bb`；S2 棋盘恢复点 `d4ed194`，协议 v2 服务端基线 `b17b520`。
- 最终客户端 5 项测试、TypeScript、真实浏览器点击/重连/格心/视角/横竖屏验收通过，浏览器零错误；服务端 20 项测试、编译和契约检查通过。
- Cocos 3.8.8 Web 正式构建与微信正式构建均成功（退出码 36）。微信静态检查通过：30 文件、3,530,535 字节，包含 1,482,126 字节的原棋盘 PNG。
- README 已更新现有场景入口、模块职责、双仓契约、校准、验收复现、构建命令和发布配置。截图与两份 JSON 结果位于 `docs/verification`。
- 当前未提供真实 AppID/WSS 配置，因此测试包使用 `touristappid`；未执行微信真机/上传/审核。未来具体阵营技能尚未定义，接口保留，快速匹配按要求关闭。
- 本次要求的重构与功能已落地。后续恢复从本提交继续，先读 README 与日志、运行两仓检查；不要重新生成替代棋盘或重做 S1–S3。

## R1：补充回归测试（2026-09-09）

- 新增 `tests/Regression.test.ts` 的 C01–C07：旧队列迟到任务、错误后恢复、双动画/掉帧、全量取消、新增动画起点、8 种画布布局、96 锚点四视角往返。
- 新增 `tsconfig.tests.json` 与 `npm run check:tests`，严格检查测试代码，不依赖 Creator 临时配置。
- 客户端累计 12 项测试全部通过，应用与测试 TypeScript 检查通过；无生产业务代码改动，本轮未重建或重跑浏览器。
- 工作区级两份文档按要求放在根目录 `../../docs/README.md`、`../../docs/测试文档.md`，包含安装/运行、完整用例矩阵、命令、43 项测试结果和设备验收边界。

## W1：公网 Web 部署与自动地址（2026-09-10）

- 将场景序列化的 localhost 地址改为空，集中解析公网同源、Creator/LAN 预览、微信无 DOM 运行时和显式覆盖地址；本地预览默认连接服务器 `81.70.145.148`。
- 新增 C08–C11 共 4 项回归，客户端累计 16 项、应用与测试类型检查通过。Web 正式构建完成，启用资源 MD5。
- 新增独立 Nginx/k3s 前端、网络规则、gzip 打包与可重复部署脚本；保留原后端 WebSocket/健康地址，发布失败可恢复原入口。
- 增加客户端 CI 和无地址替换的公网浏览器验收脚本。部署及公网实测结果将在 W2 记录；当前提交为实现恢复点。

## W2：公网网页与本机预览验收完成（2026-09-10）

- 实现恢复点 `eb8686a`；CI 发现 Nginx 非 root 临时目录权限问题后，在 `38e4cc3` 修正。前端生产版本为 `38e4cc3`，发布编号 `38e4cc3f3419-1788971140969`，打包时工作树干净。
- <http://81.70.145.148> 已提供正式 Cocos Web。Nginx Pod 1/1、0 次重启，空闲约 1m CPU / 5 MiB；原 Node/MySQL 未重启。
- 公网浏览器加载真实资源、观察实际 WebSocket 地址，无 URL 替换或认证桩；双人开房、选色、准备、开局、双骰动画和选择通过，零浏览器错误。结果和截图位于 `docs/verification/published-*`。
- 本机构建预览 `http://127.0.0.1:7459` 实际连接 `ws://81.70.145.148`，相同联机流程通过。首次预览检查遇到过 4 次握手失败且自动恢复；携带诊断复测为两个正常连接、零错误，未修改断线恢复逻辑。
- 公网原后端冒烟通过，仍返回 `c3851d7`；验证 gzip、哈希资源一年缓存与 HTML 重新验证生效。
- 客户端 CI [34376655633](https://github.com/VoidInHeart/SkillLudo_Client/actions/runs/34376655633) 通过 16 项回归、脚本语法和只读非 root Nginx 检查。公网验收脚本另修正异步启动等待，避免引擎尚未加载时提前读取控制器。
- Web 和微信正式构建通过；微信 30 文件合计 3,531,517 字节，未做微信真机/上传。前端完整构建与发布使用本机 `npm run deploy:web`，不新增 GitHub Secrets；后端自动部署仍独立运行。

## K1：期望下拉框与可撤销选骰（2026-09-11）

- 房间期望颜色改为下拉框，覆盖层拦截背景按钮，选择或点击外部关闭。
- 新增 `ActionSelection` 管理本地预选；可反复切换骰子，立即高亮服务器给出的合法飞机，点击飞机提交点数与飞机，不再弹第二次移动确认。没有可动飞机时显示显式跳过。
- 接入协议 v3，保留从旧 AI 分步状态恢复手动控制的兼容入口。
- 新增 C12–C14，客户端累计 19 项、应用与测试类型检查通过。尚未进行这轮浏览器构建验收；K2/K3 将接入技能规则、图鉴和技能界面，K4 统一更新旧浏览器测试流程。
- 用户确认：AI/托管不会主动发动技能，被动与既存强制后续效果仍生效。当前提交只为阶段恢复，暂不推送或部署。

## K2：同步技能契约（2026-09-11）

- K1 恢复点 `7a1367c`。同步服务器的技能目录、公开状态、受击响应、改点选项和缺口绕行路径类型，新增文件仍由服务端契约工具生成。
- 服务端技能规则已通过 49 项回归；本阶段客户端只同步契约，K3 将实现国名图鉴、技能面板、地图选目标和锁定/回起飞处等表现。

## K3：图鉴、技能交互与棋盘表现（2026-09-11）

- K2 恢复点：客户端 `4037aec`、服务端 `084164a`。开始页和登录页新增四国技能图鉴；对局技能面板显示觉醒、次数、冷却、强制改点和三格能量，托管时禁用主动技能。
- 改点方案仍为本地预览，点击飞机原子提交；英国选两架公共航线飞机确认换位，美国从 52 个旋转后的校准格心中选轰炸中心；法国受击选择/15 秒倒计时、巴黎救援和中国强化接入服务器。
- 新增换位/解锁/五格轰炸表现，法国锁定飞机右上角画锁，中国受击飞回起飞处；缺口绕行的负进度也使用准确格心，手动校准保留。
- 19 项客户端回归与类型检查、Web 正式构建、真实浏览器预选/移动/下拉框/重连/视角/校准通过。技能专用浏览器用例发现 Creator 会把 Set 展开编译成 concat，法国锁定提交改用 Array.from；K4 保留该回归及全部验收证据。
- 本阶段提交 UI 恢复点，K4 完成联机/技能表现回归、微信构建、配套文档与发布；尚未推送或改动线上。

## K4：回归、正式构建与文档（2026-09-11）

- K3 提交 `ad6f2a4`。更新原浏览器用例为可撤销预选/一次提交，并增加图鉴和下拉框的真实点击；新增五组服务器计算的技能表现夹具与 `verify:skills`。
- 19 项客户端、54 项服务端，共 73 项自动化通过；两仓类型检查和契约校验通过。真实本机联机与五组技能浏览器验收通过，零页面错误，落点误差 <0.01；截图/JSON 在 `docs/verification`。
- Web 与微信正式构建通过。微信 30 文件、3,556,038 字节，低于 4 MiB，仍未进行微信真机/上传。
- 两仓 README、工作区 `../docs/README.md`、`../docs/测试文档.md`、规则计划与进度日志更新。K4 发布时会固定服务端 CI 对应客户端提交；发布结果另记，旧容量报告不代表新版实测。

## K4 发布验收完成（2026-09-11）

- 客户端 `3aa8a94` 已推送并部署，发布编号 `3aa8a94eb405-1789105878006`；服务端 `f430d83` 已由 CI 自动部署，协议 v3 配套上线。客户端 CI [34567602979](https://github.com/VoidInHeart/SkillLudo_Client/actions/runs/34567602979) 成功，服务端 CI [34567642123](https://github.com/VoidInHeart/SkillLudo_Server/actions/runs/34567642123) 成功。
- 发布包工作树干净，原始 3,754,378 字节、gzip 传输约 2,045,279 字节。前后端均保留上版镜像记录，可配套回退；升级不迁移旧内存棋局。
- 公网页面 <http://81.70.145.148> 与本机构建预览均使用未经替换的实际 `ws://81.70.145.148`，双人开房/选阵营/开局/双骰预选/一次提交通过，两个浏览器各零错误；证据 `published-result.json`、`local-preview-result.json`。独立公网 v3 WebSocket 冒烟最终通过，revision 为 `f430d83`。
- 切换路由后的首次健康请求曾超时，后续收敛正常；远端本机 readyz 约 2.6 ms，Pod 均 1/1、0 重启，Node 约 32 MiB、Nginx 约 3 MiB（短时空闲观测，不是容量测试）。
- 无新增 GitHub Secret；现有后端 production 的 `DEPLOY_SSH_KEY`、`DEPLOY_KNOWN_HOSTS` 与 `DEPLOY_HOST`、`DEPLOY_USER` 已核验配置。最终文档提交不触发部署，线上代码保持上述版本。

## L1：虫洞表现与中国动态技能说明（2026-09-11）

- 上轮验收文档 `64ceb6e` 已推送。同步新储备次数/单轮使用标记和技能目录，觉醒页签显示当前进度/目标，达到不同强化等级时动态改变中国技能说明。
- 飞越虫洞时移除中间格碰撞特效，分别在起点/出口结算已由服务器声明的捕获；兼容跳入再飞或直入后再跳。
- 客户端 19 项与两类 TypeScript 检查通过，服务端 58 项通过；本阶段还未重建浏览器/微信，将随后续 L2–L5 新机制统一验收，尚未发布。
